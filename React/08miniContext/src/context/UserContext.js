import React from 'react'

const UserContext= React.createContext()

export default UserContext;
//context gives u a provider (acts as a warpper as 
// it wraps the user component and then the data can be used by all)  )